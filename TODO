
TODO:

- Delayed view cache regeneration
- Adding duplicate entries to ezm_pool: must be checked if the object_id
  is not already there
- GUI for setting publication time for items in queue.
- GUI for setting rotation validity (rotate_until) for online items.
- GUI for additional parameters for dynamic blocks
- Error handling
- Cronjob: Add the detection of another update process running (locking).
- Cronjob: Remove the items from queue which have rotate_until in the past.
- Cronjob: Generate (permanent) view cache.
- Automatic fetches
- Remove all blocks when object is removed.
- Remove all blocks when zone is removed.